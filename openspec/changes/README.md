# Changes

Active changes live here during their lifecycle. Each change has its own folder with:
- `proposal.md` (WHAT + WHY)
- `design.md` (HOW + decisions)
- `tasks.md` (STEPS for apply)
- `specs/{capability}/spec.md` (delta specs)

When a change is archived, it moves to `changes/archive/`.

Currently no active changes.
