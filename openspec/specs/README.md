# Specs

Delta specs live under `openspec/changes/{change-name}/specs/{capability}/spec.md`.
This folder holds the merged "main" specs after each change is archived.

Currently empty — populated as changes are archived.
